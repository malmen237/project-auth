import React from 'react';
import { Button } from './GlobalStyles';

const SignOutButton = ({ }) => {
  return (
    <>
      <Button signOut type="submit">
        SIGN OUT
      </Button>
    </>
  )
}

export default SignOutButton;
